
public class Solution {
	static ListNode createList(char[] chars){
		ListNode head = null;
		for(int i=0; i<chars.length; i++){
			if(head==null){
				head=new ListNode(chars[i]);
				head.prev=null;
			}else{
				ListNode temp = head;
				head=head.next;
				head=new ListNode(chars[i]);
				head.prev=temp;
			}
		}
		return head;
	}
	static char LIFO(ListNode head){
		while(head.prev!=null){
			char s=head.val;
			System.out.println(head.val);
			head=head.prev;
			return s;	
		}
		return null;
	}
	public static void main(String[] args){
		//Create linked list
		char[] c = {'a','b','c','d','e','f','g'};
		LIFO(createList(c));
	}
}
